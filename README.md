# Simple Time Service

```
.
├── app <-- app files and Dockerfile go here
└── terraform <-- Terraform files go here
```

> Each subfolder (`app/`, `terraform/`) contains its own `README.md` with detailed instructions.